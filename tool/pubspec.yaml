name: yyt_tool
description: Repository gates (docs, coverage). Not a library; never published.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  yaml: ^3.1.0
dev_dependencies:
  test: ^1.31.0
