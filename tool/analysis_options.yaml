include: ../analysis_options.yaml

# Gate scripts are not a public API; their doc comments live in the file
# header and the README.
analyzer:
  errors:
    public_member_api_docs: ignore
linter:
  rules:
    public_member_api_docs: false
    avoid_print: false
