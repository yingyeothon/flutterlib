name: yingyeothon_fake_gateway
description: In-process stand-in for the yyt gateway, for tests and the offline demo.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  yingyeothon_codec: ^0.1.0
dev_dependencies:
  test: ^1.31.0
